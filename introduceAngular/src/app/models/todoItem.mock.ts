import { TodoItem } from "./todoItem.model";

export const fakeItems: TodoItem[] = [
    new TodoItem('Çiçekleri Sula', false),
    new TodoItem('Angular Öğren', false),
    new TodoItem('ASP.NET Core ile API Yaz', true)
];